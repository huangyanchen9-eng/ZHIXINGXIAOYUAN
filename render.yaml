# Optional deployment template. Creating this service requires a paid plan.
services:
  - type: web
    name: zhixing-campus
    runtime: docker
    plan: starter
    region: singapore
    healthCheckPath: /api/health
    disk:
      name: campus-data
      mountPath: /data
      sizeGB: 1
    # Campus graph is the default; no map credentials are required.
